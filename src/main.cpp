
#include "demo/tictactoe.hpp"

int WINAPI WinMain(_In_ HINSTANCE curInst, _In_opt_ HINSTANCE prevInst, _In_ PSTR cmdLine, _In_ INT cmdCount) {
	TicTacToeDemoMain(curInst, prevInst, cmdLine, cmdCount);
}